{
  "config": {
    "step": {
      "user": {
        "title": "Camera Timelapse Setup",
        "description": "Configure the integration. Cameras are added after setup via the Configure button.",
        "data": {
          "name": "Integration name",
          "storage_path": "Storage path"
        },
        "data_description": {
          "storage_path": "Directory where timelapses and frames are saved. Use /media/timelapses for HA Media Browser access."
        }
      }
    },
    "abort": {
      "already_configured": "This storage path is already configured in another instance."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "Manage Cameras",
        "description": "Add, edit or remove cameras from timelapse tracking.",
        "data": {
          "action": "Action",
          "camera_entity_id": "Camera to edit or remove"
        }
      },
      "add_camera": {
        "title": "Add Camera",
        "description": "Select the camera entity to add.",
        "data": {
          "camera_entity_id": "Camera"
        }
      },
      "camera_settings": {
        "title": "Camera Settings",
        "description": "Configure timelapse settings for {camera_id}.",
        "data": {
          "interval_minutes": "Capture interval",
          "time_range_type": "Active time window",
          "time_start": "Start time",
          "time_end": "End time",
          "mode": "Timelapse mode",
          "rolling_period_days": "Rolling window",
          "output_format": "Output format",
          "fps": "Frames per second",
          "max_retention_days": "Max retention (0 = unlimited)",
          "assembly_interval_minutes": "Assemble timelapse every (minutes, 0 = only at end of day)",
          "hdr_frames": "Frame averaging (0 = off, 2–5 = frames to average)",
          "keep_frames": "Keep frame files on disk after assembly",
          "placeholder_image": "Unavailable camera background image (path)"
        },
        "data_description": {
          "hdr_frames": "Capture multiple frames per interval and average them to reduce noise. 0 disables this feature.",
          "keep_frames": "When disabled (streaming mode), raw JPEG frames are deleted after each assembly and new frames are appended to the existing timelapse. Saves disk space. Only compatible with Daily mode — Rolling mode always keeps frames.",
          "placeholder_image": "Path to a JPEG used as the background when the camera is unavailable. Leave empty to use the auto-generated default at {storage_path}/placeholder.jpg. The text 'Camera unavailable' is overlaid on the image at runtime."
        }
      }
    },
    "error": {
      "required": "This field is required.",
      "already_configured": "This camera is already configured."
    }
  },
  "entity": {
    "sensor": {
      "last_timelapse": {
        "name": "Last Timelapse"
      },
      "frames_today": {
        "name": "Frames Today"
      },
      "disk_usage": {
        "name": "Disk Usage"
      }
    },
    "image": {
      "latest_frame": {
        "name": "Latest Frame"
      }
    }
  },
  "services": {
    "capture_now": {
      "name": "Capture Frame Now",
      "description": "Immediately capture a frame from one or all configured cameras, bypassing the active time window."
    },
    "generate_timelapse": {
      "name": "Generate Timelapse Now",
      "description": "Manually trigger timelapse assembly for a camera."
    }
  }
}
